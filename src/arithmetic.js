class Arithmetic {
  sum(a, b) {
    return a + b;
  }

  diff(a, b) {
    return a - b;
  }

  product(a, b) {
    return a * b;
  }

  quotient(a, b) {
    return a / b;
  }
}

export default Arithmetic;
